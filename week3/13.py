import random
'''
def guess_number():
    number = random.randit(1,20)
    attempts = 0;
    print("")

    while True:
        try:
            guess = int(intput("Take a guess."))
            attempts += 1

            if guess < number:
                print ("Your guess is too low. ")
            elif guess > number:
                print("Your guess is too high. ")
            else:
                print("Good job, KBTU! You guessed my number in {attemts} guesses")
guess_number() '''